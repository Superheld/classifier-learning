# Python
.venv/
__pycache__/
*.pyc

# Generierte / regenerierbare Artefakte
# Embedding-Caches (via Track B neu erzeugbar)
*.npy
# LLM-Antwort-Caches (via Track D neu erzeugbar)
05_track_d_llm/cache/
# trainierte sklearn-Modelle
*.joblib
# persistierte Champion-Modelle (regenerierbar via Notebooks)
modelle/
# gespeicherte Vorhersagen je Modell (Quelle für Deep-Dive/Dashboard, regenerierbar)
predictions/
# Betriebszahlen-Benchmark (maschinen-spezifische Latenz, via betriebszahlen.py regenerierbar)
06_synthese/betriebszahlen.json
# Track-C-Trainingsläufe (Checkpoints/Logs, regenerierbar) — an beliebiger Ebene
runs*/
# versehentlich verschachteltes Verzeichnis (relativer output_dir aus falschem cwd)
04_track_c_finetuning/04_track_c_finetuning/

# Nebendatensaetze (nicht Kern; via data/support/download_*.py holbar)
data/support/*/

# macOS / Editor
.DS_Store
.zed/

# Isolierte MLX-venv für mlx-lm (regenerierbar via requirements; ~400M)
.venv-mlx/
# QLoRA-Adapter & gefuste Modelle je Trainingslauf (Gewichte, regenerierbar)
05_track_d_llm/lokal/ft_adapters*/
05_track_d_llm/lokal/fused*/
# generierte SFT-Trainingsdaten (train/valid.jsonl, regenerierbar aus banking77)
05_track_d_llm/lokal/ft_data/
