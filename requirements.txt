scikit-learn>=1.3
pandas>=2.0
matplotlib>=3.7
sentence-transformers>=2.7
anthropic>=0.34
# Track C (Finetuning) — Encoder auftauen via HF transformers auf MPS.
# Kommen zwar transitiv über sentence-transformers rein, hier aber direkt genutzt:
torch>=2.2
transformers>=4.40
datasets>=2.19
accelerate>=0.30
peft>=0.11          # LoRA (parameter-effizientes Finetuning, C·P3)
gradio>=5           # Dashboard (06_synthese/dashboard.py)
plotly>=5           # 3D-Embedding-Explorer im Dashboard
# Optional, je Etappe (bei Bedarf einkommentieren):
# fasttext-wheel     # E3
# setfit             # B·P3
# mlflow, optuna     # C·P3 (Experiment-Tracking-/Tuning-Upgrade)
# cleanlab, shap     # E11 / Vertiefungen
